
## Keep "We do the research so you don't have to." on one line

**One change** in `src/components/SignatureFeatures.tsx`, line 71:

Change `maxWidth: "600px"` to `maxWidth: "800px"`. This gives the headline enough room to stay on a single line at laptop widths while still preventing it from stretching too wide on large monitors.
